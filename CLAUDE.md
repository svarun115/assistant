# Personal Assistant Workspace

## Structure

```
AI Projects/Assistant/
├── CLAUDE.md                    ← you are here
├── .claude/                     ← assistant Claude things (own repo)
│   ├── skills/                  ← cos, journal, daily-tracker, fitness-coach, ...
│   ├── agents/                  ← journal-agent
│   ├── data/                    ← user-context.md, daily-context.json
│   └── plans/persistent/        ← saved plans
├── assistant-server/            ← backend + frontend (own repo)
│   ├── backend/
│   └── frontend/
└── mcp-servers/                 ← all MCP server repos
    ├── mcp-server-manager/
    ├── db-mcp-server/
    ├── garmin-mcp-server/
    ├── google-workspace-mcp-server/
    ├── googleplaces-mcp-server/
    └── splitwise-mcp-server/
```
