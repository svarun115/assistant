# Create ADO — Retro Context

Skill-specific context for `/retro create-ado` sessions.

---

## GitHub Issue Repos

| Server | Repo | Labels |
|--------|------|--------|
| ADO MCP | Microsoft Agency CLI (internal) | File via `agency` feedback or internal channels |

---

## Skill-Specific Retro Notes

Common root causes in create-ado sessions:

- **Wrong field**: Area path or iteration path mismatches
- **Schema gap**: Missing fields or work item types not supported by the MCP tool
