# Environment Configuration for Journal Database

# ============================================================================
# Application Environment
# ============================================================================

# Environment mode: development, test, or production
# - development: Uses local PostgreSQL (DB_NAME)
# - test: Uses test database (TEST_DB_NAME or 'personal_journal_test')
# - production: Uses production database with strict SSL
APP_ENV=development

# ============================================================================
# Database Configuration (Development/Production)
# ============================================================================

# Local Development PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=journal_db
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_SSL_MODE=prefer

# Azure Production (uncomment and update when deploying)
# DB_HOST=your-server.postgres.database.azure.com
# DB_PORT=5432
# DB_NAME=journal_db
# DB_USER=your_admin_user@your-server
# DB_PASSWORD=your_secure_password
# DB_SSL_MODE=require

# Connection Pool Settings
DB_MIN_POOL_SIZE=2
DB_MAX_POOL_SIZE=10

# ============================================================================
# Test Database Configuration (Optional)
# ============================================================================

# Override test database settings (defaults to local with "_test" suffix)
# TEST_DB_HOST=localhost
# TEST_DB_PORT=5432
# TEST_DB_NAME=personal_journal_test
# TEST_DB_USER=postgres
# TEST_DB_PASSWORD=your_password_here

# ============================================================================
# Application Settings
# ============================================================================

# Query Mode for read operations
# sql: LLM writes raw SQL via execute_sql_query (default)
# structured: LLM uses typed query/aggregate tools (server generates SQL)
QUERY_MODE=sql

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# ============================================================================
# Azure Configuration (Optional)
# ============================================================================

# Azure Key Vault (for production secrets)
# AZURE_KEY_VAULT_URL=https://your-keyvault.vault.azure.net/
# AZURE_TENANT_ID=your-tenant-id
# AZURE_CLIENT_ID=your-client-id
# AZURE_CLIENT_SECRET=your-client-secret
