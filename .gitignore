# ─── Secrets — never commit these ────────────────────────────────────────────
.env.production
.env.local

# OAuth tokens
tokens/

# ─── Separate repos — not tracked here ───────────────────────────────────────
# MCP servers have their own repos (svarun115/journal-db-mcp-server, etc.)
# Clone them locally into mcp-servers/ but they are not part of this repo.
mcp-servers/

# Personal Claude config has its own private repo (svarun115/claude-personal)
.claude/

# ─── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# ─── Node ─────────────────────────────────────────────────────────────────────
node_modules/

# ─── SQLite state (local dev only — production uses PostgreSQL) ───────────────
*.db
*.db-shm
*.db-wal

# ─── Logs ─────────────────────────────────────────────────────────────────────
logs/
llm_logs/
query_logs/

# ─── Test / build artifacts ───────────────────────────────────────────────────
backups_test/
chroma_db/

# ─── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
