# ─── Secrets — never commit these ────────────────────────────────────────────
.env.production
.env.local

# MCP server local credential files
mcp-servers/**/.env
mcp-servers/**/.env.development
mcp-servers/**/.env.production
mcp-servers/**/.env.test

# Google OAuth tokens (local auth files — copied to VM via scp)
tokens/
mcp-servers/google-workspace-mcp-server/token.json
mcp-servers/google-workspace-mcp-server/credentials.json

# Personal user data (copied to VM via scp, like .env.production)
.claude/data/

# ─── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# ─── Node ─────────────────────────────────────────────────────────────────────
node_modules/

# ─── SQLite state (local dev only — production uses PostgreSQL) ───────────────
*.db
*.db-shm
*.db-wal

# ─── Logs ─────────────────────────────────────────────────────────────────────
logs/
llm_logs/
query_logs/

# ─── Test / build artifacts ───────────────────────────────────────────────────
backups_test/
chroma_db/

# ─── OS ───────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
